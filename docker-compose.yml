services:
  nong-isel:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    container_name: nong-isel
    volumes:
      - ./ticket_id.txt:/usr/src/bot/ticket_id.txt
      - ./message_id.txt:/usr/src/bot/message_id.txt
